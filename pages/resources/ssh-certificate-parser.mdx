---
title: SSH Certificate Parser
subtitle: Teleport Access Plane
description: "SSH Certificate Parser: Free online decoder for SSH Certificates using Teleport: Modern SSH for Teams Managing Distributed Server Clusters"
headerBehaviour: floating
headerMode: full
hideWave: true
border: none
---

import PageIntro from "components/PageIntro";
import SSHParser from "components/SSHParser";
import Section from "components/Section";
import Centrator from "components/Centrator";
import TryTeleport from "components/TryTeleport";
import SectionHeader from "components/SectionHeader";

<SectionHeader
  bg="wave-on-gray"
  description={meta.description}
  descriptionTextWidth="1240px"
  kind="column"
  mode="none"
  subtitle={meta.subtitle}
  title={meta.title}
/>

<Section bg="flatWhite" width="100%" borderTop="1px solid #F0F2F4">
  <Centrator py={[3, 5]}>
    <Section bg="flatWhite" fontColor="darkest" lineHeight="md" fontSize="text-md" maxWidth="1240px">
      <PageIntro
        title="Decoding SSH Certificates"
        description="Use this SSH Parser to decode your SSH certificates to verify it contains the correct information. Check to see certificate type, public key, signing authority, dates of validity etc. Try our sample certificate to get started."
        renderSmallHeading={true}
        verticalResponsive={false}
      />
    </Section>
  </Centrator>

  <Section bg="flatGray" width="100vw" justifyContent="center">
    <Centrator py={[3, 5]}>
      <SSHParser />
    </Centrator >
  </Section>
</Section>

<TryTeleport/>
