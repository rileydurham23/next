---
title: Installing Teleport from Source
subtitle: Community Edition
headerBehaviour: floating
headerMode: full
border: none
hideWave: true
description: "How to install Teleport Community Edition: Implement industry-best practices for SSH and Kubernetes access, meet compliance requirements"
---

import styled from "styled-components";
import css from "@styled-system/css";

import Box from "components/Box";
import Centrator from "components/Centrator";
import ForkRibbon from "components/ForkRibbon";
import Link from "components/Link";
import Section from "components/Section";
import SectionHeader from "components/SectionHeader";
import Terminal from "components/Terminal";

<SectionHeader
  title={meta.title}
  subtitle={meta.subtitle}
  description={meta.description}
  verticalResponsive={false}
  bg="wave-on-gray"
  kind="row"
>
  <Box>
    <h3>Additional ways to install Teleport</h3>
    <ul>
      <li>
        <Link
          href="https://goteleport.com/docs/getting-started/"
          color="light-purple"
        >
          Teleport Quickstart Guide
        </Link>
      </li>
      <li>
        <Link
          href="https://goteleport.com/teleport/installing"
          color="light-purple"
        >
          Install from Source
        </Link>
      </li>
      <li>
        <Link
          href="https://quay.io/repository/gravitational/teleport?tab=tags"
          color="light-purple"
        >
          Install using Docker
        </Link>
      </li>
    </ul>
  </Box>
</SectionHeader>

<ForkRibbon repoLink="https://github.com/gravitational/teleport"/>
<Centrator>
  <Section bg="flatWhite" pt={5} css={css({ p: { fontSize: "text-xl" } })}>

Make sure you have Golang `v1.14` or newer, then run:

      <Terminal mb={4}>
        ```bash
        # get the source and build
        $ mkdir -p $GOPATH/src/github.com/gravitational
        $ cd $GOPATH/src/github.com/gravitational
        $ git clone https://github.com/gravitational/teleport.git

        # create the default data directory:

        $ sudo mkdir -p /var/lib/teleport
        $ sudo chown $USER /var/lib/teleport

        # build:
        $ cd teleport
        $ make full
        ```
      </Terminal>

If the build succeeds the binaries will be placed in `$GOPATH/src/github.com/gravitational/teleport/build`

### OTHER USEFUL MAKE TARGETS

      <Terminal mb={6}>
        ```bash
        # build using Docker. in this case golang is not required:
        $ make -C build.assets full

        # generate release tarball using Docker:
        $ make -C build.assets release
        ```
      </Terminal>
    </Section>

</Centrator>
